cookiecutter:
  templates:
    - template: gh:inetum-orleans/djp-node
      extra_context:
        node_version: "22"
app:
  db:
    user: 'user'
    password: 'password'
    db: 'database'
shell:
  aliases:
    dc: docker compose -f $DDB_CORE_PATH_PROJECT_HOME/docker-compose.yml